export interface ICard {
    title?: string;
    label?: string;
    instructions?: string;
    buttonText?: string;
    isLeft?: boolean;

    backTitle?: string;
    upperInput?: string;
    upperPlaceholder?: string;
    lowerInput?: string;
    lowerPlaceholder?: string;
}
